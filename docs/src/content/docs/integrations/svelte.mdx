---
title: "@witheslint/preset-svelte"
sidebar:
  label: Svelte
---
import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

✨ The svelte preset for witheslint.

<Aside type="tip">
  This preset is included in the `witheslint` package
</Aside>

### Installation

<Tabs>
  <TabItem label="pnpm">
  ```sh
  pnpm add -D eslint witheslint
  # pnpm add -D eslint @witheslint/core @witheslint/preset-svelte
  ```
  </TabItem>
  <TabItem label="yarn">
  ```sh
  yarn add -D eslint witheslint
  # yarn add -D eslint @witheslint/core @witheslint/preset-svelte
  ```
  </TabItem>
  <TabItem label="npm">
  ```sh
  npm install -D eslint witheslint
  # npm install -D eslint @witheslint/core @witheslint/preset-svelte
  ```
  </TabItem>
</Tabs>

### Reference

<Tabs>
  <TabItem label="ESModule">
  ```js
  // eslint.config.js
  import { defineConfig, presetSvelte } from 'witheslint'
  // import { defineConfig } from '@witheslint/core'
  // import { presetSvelte } from '@witheslint/preset-svelte'

  export default defineConfig({
    presets: [
      presetSvelte(),
      // ...other presets
    ],
  })
  ```
  </TabItem>
  <TabItem label="CommonJS">
  ```js
  // eslint.config.js
  module.exports = (async () => {
    const { defineConfig, presetSvelte } = await import('witheslint')
    // const { defineConfig } = await import('@witheslint/core')
    // const { presetSvelte } = await import('@witheslint/preset-svelte')

    return defineConfig({
      presets: [
        presetSvelte(),
        // ...other presets
      ],
    })
  })()
  ```
  </TabItem>
</Tabs>

> See [customization](/guides/customization) for more details.
