---
title: "@witheslint/preset-vue"
sidebar:
  label: Vue
---
import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

✨ Use vue eslint config preset in witheslint. ✨

<Aside type="tip">
  This preset is included in the `witheslint` package
</Aside>

### Installation

<Tabs>
  <TabItem label="pnpm">
  ```sh
  pnpm add -D eslint witheslint
  # pnpm add -D eslint @witheslint/core @witheslint/preset-vue
  ```
  </TabItem>
  <TabItem label="yarn">
  ```sh
  yarn add -D eslint witheslint
  # yarn add -D eslint @witheslint/core @witheslint/preset-vue
  ```
  </TabItem>
  <TabItem label="npm">
  ```sh
  npm install -D eslint witheslint
  # npm install -D eslint @witheslint/core @witheslint/preset-vue
  ```
  </TabItem>
</Tabs>

### Reference

<Tabs>
  <TabItem label="ESModule">
  ```js
  // eslint.config.js
  import { defineConfig, presetVue } from 'witheslint'
  // import { defineConfig } from '@witheslint/core'
  // import { presetVue } from '@witheslint/preset-vue'

  export default defineConfig({
    presets: [
      presetVue(),
      // ...other presets
    ],
  })
  ```
  </TabItem>
  <TabItem label="CommonJS">
  ```js
  // eslint.config.js
  module.exports = (async () => {
    const { defineConfig, presetVue } = await import('witheslint')
    // const { defineConfig } = await import('@witheslint/core')
    // const { presetVue } = await import('@witheslint/preset-vue')

    return defineConfig({
      presets: [
        presetVue(),
        // ...other presets
      ],
    })
  })()
  ```
  </TabItem>
</Tabs>

> See [customization](/guides/customization) for more details.
